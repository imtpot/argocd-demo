apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/

namePrefix: stage-

patches:
  - path: bootstrap-patch.yaml
    target:
      kind: Application
      name: bootstrap
  - path: podinfo-patch.yaml
    target:
      kind: Application
      name: podinfo
